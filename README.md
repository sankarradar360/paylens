# PayLens

Backend-only scaffold: FastAPI backend with Docker.

Prerequisites:
- Docker & docker-compose
- Python 3.11+

Run backend locally with docker-compose:

```bash
docker-compose up --build
```

Backend: http://localhost:8000
